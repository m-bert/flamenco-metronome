import { Dimensions } from "react-native";

export const numberOfBeats = 12;

export const clockRadius = 120;

export const screenCenter = {
  x: Dimensions.get('window').width / 2,
  y: Dimensions.get('window').height / 2,
};

